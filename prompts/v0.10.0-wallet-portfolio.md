# Chain Indexer v0.10.0 - Wallet Portfolio Feature

## Overview

Implementasi fitur **Wallet Portfolio** untuk chain-indexer. Fitur ini memungkinkan user untuk melihat portfolio holdings sebuah wallet address, termasuk semua token yang dimiliki, balance, dan nilai USD.

## Git Flow

```
main → feature/wallet-portfolio → PR → merge → tag v0.10.0
```

## Current State (v0.9.1)

### Existing Entities
- `Transfer` - ERC-20 transfer events
- `Token` - Token metadata (address, name, symbol, decimals)
- `IndexerState` - Indexing progress per token

### Existing Services
- `TransferService` - Query transfers
- `TokenService` - Query tokens
- `StatsService` - Token statistics (volume, transfers count)
- `HoldersService` - Top holders, holder balance per token

### Existing Endpoints
```
GET /api/v1/transfers
GET /api/v1/tokens
GET /api/v1/tokens/{address}
GET /api/v1/tokens/{address}/stats
GET /api/v1/tokens/{address}/holders
GET /api/v1/tokens/{address}/holders/{holder}
GET /api/v1/tokens/{address}/holder-count
```

---

## Implementation Tasks

### Phase 1: Core Portfolio Entity & Repository

#### 1.1 Create Portfolio Entity
**File:** `internal/domain/entities/portfolio.go`

```go
package entities

import (
    "math/big"
    "time"
)

// TokenHolding represents a single token holding in a portfolio
type TokenHolding struct {
    TokenAddress string    `json:"token_address"`
    TokenName    string    `json:"token_name"`
    TokenSymbol  string    `json:"token_symbol"`
    Decimals     int       `json:"decimals"`
    Balance      *big.Int  `json:"-"`
    BalanceStr   string    `json:"balance"`           // Raw balance (wei)
    BalanceHuman string    `json:"balance_formatted"` // Human readable (with decimals)
    PriceUSD     *float64  `json:"price_usd,omitempty"`
    ValueUSD     *float64  `json:"value_usd,omitempty"`
    Percentage   *float64  `json:"percentage,omitempty"` // % of total portfolio
}

// WalletPortfolio represents complete portfolio for a wallet
type WalletPortfolio struct {
    WalletAddress   string         `json:"wallet_address"`
    Holdings        []TokenHolding `json:"holdings"`
    TotalValueUSD   *float64       `json:"total_value_usd,omitempty"`
    TotalTokens     int            `json:"total_tokens"`
    LastUpdated     time.Time      `json:"last_updated"`
}

// PortfolioFilter untuk query portfolio
type PortfolioFilter struct {
    WalletAddress string
    TokenAddress  *string  // Optional: filter specific token
    MinBalance    *big.Int // Optional: minimum balance filter
    IncludeZero   bool     // Include zero balance tokens
}
```

#### 1.2 Create Portfolio Repository Interface
**File:** `internal/domain/repositories/portfolio_repository.go`

```go
package repositories

import (
    "context"

    "github.com/bimakw/chain-indexer/internal/domain/entities"
)

// PortfolioRepository defines interface for portfolio data operations
type PortfolioRepository interface {
    // GetWalletHoldings retrieves all token holdings for a wallet
    // Calculates balance from transfers: SUM(received) - SUM(sent)
    GetWalletHoldings(ctx context.Context, walletAddress string) ([]entities.TokenHolding, error)

    // GetWalletHoldingByToken retrieves holding for specific token
    GetWalletHoldingByToken(ctx context.Context, walletAddress, tokenAddress string) (*entities.TokenHolding, error)

    // GetWalletTokenCount returns count of tokens held by wallet
    GetWalletTokenCount(ctx context.Context, walletAddress string) (int64, error)

    // GetWalletTransferSummary returns transfer stats for a wallet
    GetWalletTransferSummary(ctx context.Context, walletAddress string) (*WalletTransferSummary, error)
}

// WalletTransferSummary holds transfer statistics for a wallet
type WalletTransferSummary struct {
    TotalTransfersIn   int64
    TotalTransfersOut  int64
    TotalVolumeIn      string
    TotalVolumeOut     string
    UniqueTokens       int64
    FirstTransferAt    *time.Time
    LastTransferAt     *time.Time
}
```

#### 1.3 Implement PostgreSQL Portfolio Repository
**File:** `internal/infrastructure/database/portfolio_repo.go`

Key SQL queries:
```sql
-- Get all holdings for a wallet (balance = received - sent)
WITH balances AS (
    SELECT
        token_address,
        SUM(CASE WHEN to_address = $1 THEN value ELSE 0 END) -
        SUM(CASE WHEN from_address = $1 THEN value ELSE 0 END) as balance
    FROM transfers
    WHERE from_address = $1 OR to_address = $1
    GROUP BY token_address
    HAVING SUM(CASE WHEN to_address = $1 THEN value ELSE 0 END) -
           SUM(CASE WHEN from_address = $1 THEN value ELSE 0 END) > 0
)
SELECT
    b.token_address,
    t.name,
    t.symbol,
    t.decimals,
    b.balance::text
FROM balances b
JOIN tokens t ON t.address = b.token_address
ORDER BY b.balance DESC;

-- Get wallet transfer summary
SELECT
    COUNT(*) FILTER (WHERE to_address = $1) as total_in,
    COUNT(*) FILTER (WHERE from_address = $1) as total_out,
    COALESCE(SUM(value) FILTER (WHERE to_address = $1), 0)::text as volume_in,
    COALESCE(SUM(value) FILTER (WHERE from_address = $1), 0)::text as volume_out,
    COUNT(DISTINCT token_address) as unique_tokens,
    MIN(block_timestamp) as first_transfer,
    MAX(block_timestamp) as last_transfer
FROM transfers
WHERE from_address = $1 OR to_address = $1;
```

---

### Phase 2: Portfolio Service

#### 2.1 Create Portfolio Service
**File:** `internal/application/services/portfolio_service.go`

```go
package services

// PortfolioService provides business logic for wallet portfolios
type PortfolioService struct {
    portfolioRepo repositories.PortfolioRepository
    tokenRepo     repositories.TokenRepository
    cache         *cache.RedisCache
    logger        *zap.Logger
}

// PortfolioDTO is the API representation
type PortfolioDTO struct {
    WalletAddress string            `json:"wallet_address"`
    Holdings      []TokenHoldingDTO `json:"holdings"`
    Summary       PortfolioSummary  `json:"summary"`
    UpdatedAt     string            `json:"updated_at"`
}

type TokenHoldingDTO struct {
    TokenAddress    string  `json:"token_address"`
    TokenName       string  `json:"token_name"`
    TokenSymbol     string  `json:"token_symbol"`
    Decimals        int     `json:"decimals"`
    Balance         string  `json:"balance"`           // Raw wei
    BalanceFormatted string `json:"balance_formatted"` // Human readable
}

type PortfolioSummary struct {
    TotalTokens       int   `json:"total_tokens"`
    TotalTransfersIn  int64 `json:"total_transfers_in"`
    TotalTransfersOut int64 `json:"total_transfers_out"`
}

// Methods to implement:
// - GetPortfolio(ctx, walletAddress) (*PortfolioDTO, error)
// - GetPortfolioByToken(ctx, walletAddress, tokenAddress) (*TokenHoldingDTO, error)
// - GetWalletSummary(ctx, walletAddress) (*PortfolioSummary, error)
```

**Caching Strategy:**
- Portfolio: 2 minute TTL (balances change frequently)
- Summary: 5 minute TTL

---

### Phase 3: API Handlers

#### 3.1 Create Portfolio Handler
**File:** `internal/presentation/handlers/portfolio_handler.go`

```go
// PortfolioHandler handles HTTP requests for portfolio endpoints
type PortfolioHandler struct {
    portfolioService *services.PortfolioService
    logger           *zap.Logger
}

// Endpoints:
// GET /api/v1/wallets/{address}/portfolio
// GET /api/v1/wallets/{address}/portfolio/tokens/{tokenAddress}
// GET /api/v1/wallets/{address}/summary
```

#### 3.2 Register Routes
**File:** `cmd/api/main.go`

Add to router:
```go
r.Route("/api/v1/wallets", func(r chi.Router) {
    r.Get("/{address}/portfolio", portfolioHandler.GetPortfolio)
    r.Get("/{address}/portfolio/tokens/{tokenAddress}", portfolioHandler.GetTokenHolding)
    r.Get("/{address}/summary", portfolioHandler.GetWalletSummary)
})
```

---

### Phase 4: Tests

#### 4.1 Unit Tests
- `internal/infrastructure/database/portfolio_repo_test.go`
- `internal/application/services/portfolio_service_test.go`
- `internal/presentation/handlers/portfolio_handler_test.go`

#### 4.2 Test Fixtures
Update `internal/testutil/fixtures.go` with portfolio test data.

---

## API Specification

### GET /api/v1/wallets/{address}/portfolio

Get complete portfolio for a wallet address.

**Request:**
```
GET /api/v1/wallets/0x1234.../portfolio
```

**Response:**
```json
{
    "data": {
        "wallet_address": "0x1234...",
        "holdings": [
            {
                "token_address": "0xdac17f958d2ee523a2206206994597c13d831ec7",
                "token_name": "Tether USD",
                "token_symbol": "USDT",
                "decimals": 6,
                "balance": "1000000000",
                "balance_formatted": "1000.000000"
            },
            {
                "token_address": "0xa0b86991c6218b36c1d19d4a2e9eb0ce3606eb48",
                "token_name": "USD Coin",
                "token_symbol": "USDC",
                "decimals": 6,
                "balance": "500000000",
                "balance_formatted": "500.000000"
            }
        ],
        "summary": {
            "total_tokens": 2,
            "total_transfers_in": 150,
            "total_transfers_out": 75
        },
        "updated_at": "2026-01-07T12:00:00Z"
    }
}
```

### GET /api/v1/wallets/{address}/portfolio/tokens/{tokenAddress}

Get specific token holding for a wallet.

**Response:**
```json
{
    "data": {
        "token_address": "0xdac17f958d2ee523a2206206994597c13d831ec7",
        "token_name": "Tether USD",
        "token_symbol": "USDT",
        "decimals": 6,
        "balance": "1000000000",
        "balance_formatted": "1000.000000"
    }
}
```

### GET /api/v1/wallets/{address}/summary

Get transfer summary for a wallet.

**Response:**
```json
{
    "data": {
        "wallet_address": "0x1234...",
        "total_transfers_in": 150,
        "total_transfers_out": 75,
        "total_volume_in": "5000000000000",
        "total_volume_out": "2500000000000",
        "unique_tokens": 5,
        "first_transfer_at": "2024-01-15T10:30:00Z",
        "last_transfer_at": "2026-01-07T08:45:00Z"
    }
}
```

---

## File Structure (New Files)

```
internal/
├── domain/
│   ├── entities/
│   │   └── portfolio.go          # NEW
│   └── repositories/
│       └── portfolio_repository.go # NEW
├── infrastructure/
│   └── database/
│       └── portfolio_repo.go      # NEW
├── application/
│   └── services/
│       └── portfolio_service.go   # NEW
│       └── portfolio_service_test.go # NEW
├── presentation/
│   └── handlers/
│       └── portfolio_handler.go   # NEW
│       └── portfolio_handler_test.go # NEW
└── testutil/
    └── fixtures.go               # UPDATE - add portfolio fixtures
    └── mocks.go                  # UPDATE - add portfolio mocks

cmd/
└── api/
    └── main.go                   # UPDATE - register portfolio routes
```

---

## Implementation Order

1. **Create entity** (`portfolio.go`)
2. **Create repository interface** (`portfolio_repository.go`)
3. **Implement PostgreSQL repository** (`portfolio_repo.go`)
4. **Create service** (`portfolio_service.go`)
5. **Create handler** (`portfolio_handler.go`)
6. **Update main.go** - register routes
7. **Update mocks** (`mocks.go`)
8. **Write unit tests**
9. **Run tests & lint**
10. **Create PR & merge**
11. **Tag v0.10.0**

---

## Git Commands

```bash
# Start feature branch
git checkout main
git pull origin main
git checkout -b feature/wallet-portfolio

# After implementation
git add .
git commit -m "feat: add wallet portfolio endpoints

- Add portfolio entity and repository interface
- Implement PostgreSQL portfolio repository
- Add portfolio service with caching
- Add portfolio HTTP handlers
- Add unit tests for all components
- New endpoints:
  - GET /api/v1/wallets/{address}/portfolio
  - GET /api/v1/wallets/{address}/portfolio/tokens/{tokenAddress}
  - GET /api/v1/wallets/{address}/summary"

git push -u origin feature/wallet-portfolio

# Create PR
gh pr create --title "feat: add wallet portfolio feature" --body "..."

# After merge
git checkout main
git pull origin main
git tag -a v0.10.0 -m "v0.10.0 - Wallet Portfolio Feature"
git push origin v0.10.0
```

---

## Validation Checklist

- [ ] All new files follow existing code patterns
- [ ] Repository interface follows existing pattern (TransferRepository)
- [ ] Service uses Redis caching like existing services
- [ ] Handler uses Chi router patterns
- [ ] Error handling consistent with existing handlers
- [ ] Unit tests cover happy path and error cases
- [ ] `go test ./...` passes
- [ ] `make lint` passes
- [ ] No new dependencies required (use existing: chi, sqlx, zap, redis)
- [ ] README updated with new endpoints (optional)

---

## Notes

- Balance calculation: `SUM(received) - SUM(sent)` from transfers table
- Only show tokens with positive balance (filter zero/negative)
- Use existing Token metadata from tokens table
- Human readable balance = balance / 10^decimals
- Address validation: must be valid Ethereum address (42 chars, 0x prefix)
- All addresses should be lowercased before processing
